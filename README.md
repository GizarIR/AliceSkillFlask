# AliceSkillFlask

Steps for start app:
1) Install venv: python -m venv venv
2) Start venv: source venv/bin/activate
3) pip install -r requirements.txt
4) Start application: python app.py
5) Start new terminal and start ngrok: ngrok http 5000
6) Move https address ngrok to option Webhook in your Alice Skill
